apiVersion: networking.gke.io/v1
kind: GCPBackendPolicy
metadata:
  name: evm-mcp-server
spec:
  default:
    securityPolicy: mcp-gateway
  targetRef:
    group: ""
    kind: Service
    name: evm-mcp-server